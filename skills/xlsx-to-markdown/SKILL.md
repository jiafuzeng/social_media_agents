# XLSX To Markdown

## 适用场景

收到 `.xlsx` 文件，需要把工作表内容整理成可直接回复给用户的 Markdown 文本。

## 输入

- 已下载到受控工作区的 `.xlsx` 文件。

## 输出

- 工作簿名称。
- 每个工作表的名称。
- 最多100行、20列的 Markdown 表格文本。

## 边界

- 使用只读、`data_only` 模式读取。
- 不执行公式，不修改原文件。
- 超出预览上限的内容不进入 IM 文本回复。
