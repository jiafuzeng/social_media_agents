# Markdown To PDF

## 适用场景

收到 `.md` 或 `.markdown` 文件，需要生成 PDF 产物，并把处理后的文本预览回复给用户。

## 输入

- UTF-8 编码的 Markdown 文件。

## 输出

- PDF 文件，保存在服务工作区的 `artifacts/` 目录。
- 转换说明与 Markdown 文本预览。

## 边界

- 支持标题、普通段落和列表的基础排版。
- 课程示例只返回文本与产物路径，不伪装成已经完成外部文件上传。
- 生产环境应把产物交给对象存储或 IM 文件上传接口。
